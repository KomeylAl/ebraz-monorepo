x-app-image: &app-image ebraz-clinic-app:latest

x-app-build: &app-build
  context: .
  dockerfile: Dockerfile
  target: runner
  args:
    NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-http://localhost:4000}

services:
  migrate:
    build:
      context: .
      dockerfile: Dockerfile
      target: migrator
    env_file: .env
    command: pnpm --filter @ebraz/database run db:migrate:docker
    restart: "no"

  api:
    build: *app-build
    image: *app-image
    env_file: .env
    ports:
      - "${API_PORT:-4000}:4000"
    environment:
      NODE_ENV: production
      PORT: 4000
      HOSTNAME: 0.0.0.0
      UPLOAD_ROOT: /app/packages/api/public/uploads
    volumes:
      - uploads_data:/app/packages/api/public/uploads
      - backups_data:/app/packages/api/public/backups
    command: pnpm --filter @ebraz/api exec next start --port 4000
    depends_on:
      migrate:
        condition: service_completed_successfully
    restart: unless-stopped

  website:
    build: *app-build
    image: *app-image
    env_file: .env
    ports:
      - "${WEBSITE_PORT:-3000}:3000"
    environment:
      NODE_ENV: production
      PORT: 3000
      HOSTNAME: 0.0.0.0
    command: pnpm --filter @ebraz/website exec next start --port 3000
    depends_on:
      - api
    restart: unless-stopped

  admin:
    build: *app-build
    image: *app-image
    env_file: .env
    ports:
      - "${ADMIN_PORT:-3001}:3001"
    environment:
      NODE_ENV: production
      PORT: 3001
      HOSTNAME: 0.0.0.0
    command: pnpm --filter @ebraz/admin-panel exec next start --port 3001
    depends_on:
      - api
    restart: unless-stopped

  therapist:
    build: *app-build
    image: *app-image
    env_file: .env
    ports:
      - "${THERAPIST_PORT:-3002}:3002"
    environment:
      NODE_ENV: production
      PORT: 3002
      HOSTNAME: 0.0.0.0
    command: pnpm --filter @ebraz/therapist-panel exec next start --port 3002
    depends_on:
      - api
    restart: unless-stopped

  client:
    build: *app-build
    image: *app-image
    env_file: .env
    ports:
      - "${CLIENT_PORT:-3003}:3003"
    environment:
      NODE_ENV: production
      PORT: 3003
      HOSTNAME: 0.0.0.0
    command: pnpm --filter @ebraz/client-panel exec next start --port 3003
    depends_on:
      - api
    restart: unless-stopped

  seed:
    profiles: ["seed"]
    build:
      context: .
      dockerfile: Dockerfile
      target: migrator
    env_file: .env
    environment:
      SEED_DEV_USERS: "false"
    command: >
      sh -c "pnpm --filter @ebraz/database run db:migrate:docker
      && pnpm --filter @ebraz/database run db:seed:docker"
    restart: "no"

volumes:
  uploads_data:
  backups_data:
